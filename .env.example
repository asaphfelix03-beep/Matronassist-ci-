# Local development (SQLite)
# DATABASE_URL=file:./prisma/dev.db

# Production (Postgres) - set this in Vercel/GitHub secrets
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Vercel deployment tokens (set as repository secrets)
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=
